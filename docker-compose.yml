version: '3.8'

services:
  app:
    image: unifesto-backend
    build: .
    container_name: unifesto-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production